prepare:
  stage: dev
  script:
    - mkdir workspace
    - echo hello > workspace/test.txt

  artifacts:
    paths:
      - workspace

docker:
  stage: dev
  dependencies:
    - prepare

  script:
    - pwd
    - ls -la
    - ls -la workspace